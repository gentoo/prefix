--- configure
+++ configure
@@ -4657,6 +4657,7 @@
	fi
      esac
      shared_flag='-shared'
+      shared_flag="$shared_flag "'${wl}-G'
    else
      # not using gcc
      if test "$host_cpu" = ia64; then
