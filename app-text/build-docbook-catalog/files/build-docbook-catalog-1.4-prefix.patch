--- build-docbook-catalog-1.4.orig	2008-09-24 20:38:22.773302921 +0200
+++ build-docbook-catalog-1.4	2008-09-24 20:41:24.103020611 +0200
@@ -1,4 +1,4 @@
-#!/bin/bash
+#!/usr/bin/env bash
 # $Header: /var/cvsroot/gentoo-src/build-docbook-catalog/build-docbook-catalog,v 1.4 2008/09/22 14:10:17 flameeyes Exp $
 #
 # build-docbook-catalog: populate /etc/xml/docbook based in
@@ -9,9 +9,9 @@
 # written by Aron Griffis
 #
 
-ROOTCATALOG=/etc/xml/catalog
-CATALOG=/etc/xml/docbook
-DOCBOOKDIR=/usr/share/sgml/docbook
+ROOTCATALOG=@GENTOO_PORTAGE_EPREFIX@/etc/xml/catalog
+CATALOG=@GENTOO_PORTAGE_EPREFIX@/etc/xml/docbook
+DOCBOOKDIR=@GENTOO_PORTAGE_EPREFIX@/usr/share/sgml/docbook
 DTDS=
 LATEST_DTD=
 LATEST_DATE=
@@ -80,7 +80,7 @@
 create_catalogs() {
 	if [[ ! -r ${ROOTCATALOG} ]] ; then
 		echo "Creating XML Catalog root ${ROOTCATALOG}"
-		/usr/bin/xmlcatalog --noout --create ${ROOTCATALOG}
+		@GENTOO_PORTAGE_EPREFIX@/usr/bin/xmlcatalog --noout --create ${ROOTCATALOG}
 		if [[ ! -r ${ROOTCATALOG} ]] ; then
 			echo "Failed creating ${ROOTCATALOG}, aborting" >&2
 			exit 1
@@ -94,7 +94,7 @@
 
 	if [[ ! -r ${CATALOG} ]] ; then
 		echo "Creating DocBook XML Catalog ${CATALOG}"
-		/usr/bin/xmlcatalog --noout --create ${CATALOG}
+		@GENTOO_PORTAGE_EPREFIX@/usr/bin/xmlcatalog --noout --create ${CATALOG}
 		if [[ ! -r ${CATALOG} ]] ; then
 			echo "Failed creating ${CATALOG}, aborting" >&2
 			exit 1
