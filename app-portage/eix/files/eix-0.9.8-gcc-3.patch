--- src/portage/keywords.h
+++ src/portage/keywords.h
@@ -96,9 +96,9 @@
 		bool isSystem() const
 		{ return m_mask & SYSTEM_PACKAGE; }
 
-		void operator |= (const KeywordsFlags::Type &t)
+		void operator |= (const KeywordsFlags::Type t)
 		{ m_mask |= t; }
-		void operator &= (const KeywordsFlags::Type &t)
+		void operator &= (const KeywordsFlags::Type t)
 		{ m_mask &= t; }
 };
 
