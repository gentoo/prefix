excerpt from:
https://projects.gentooexperimental.org/eix/changeset?format=diff&new=813&old=812&new_path=%2F&old_path=%2F
http://bugs.gentoo.org/show_bug.cgi?id=255711

Index: /trunk/src/portage/conf/portagesettings.cc
===================================================================
--- /trunk/src/portage/conf/portagesettings.cc (revision 808)
+++ /trunk/src/portage/conf/portagesettings.cc (revision 813)
@@ -300,5 +300,8 @@
 					if(s > 1)
 						sets_dirs.insert(sets_dirs.begin() + i, s - 1, "");
-					for(vector<string>::const_reverse_iterator it = overlays.rbegin();
+					// The following should actually be const_reverse_iterator,
+					// but some compilers would then need a cast of rend(),
+					// see http://bugs.gentoo.org/show_bug.cgi?id=255711
+					for(vector<string>::reverse_iterator it = overlays.rbegin();
 						it != overlays.rend(); ++it)
 						sets_dirs[i++] = (*it) + app;
