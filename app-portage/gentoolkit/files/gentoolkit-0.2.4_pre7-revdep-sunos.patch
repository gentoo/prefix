--- src/revdep-rebuild/revdep-rebuild.orig	2008-05-06 16:31:48.000000000 +0200
+++ src/revdep-rebuild/revdep-rebuild	2008-05-07 08:58:48.293017000 +0200
@@ -169,6 +169,21 @@
 			echo $(ldd "$1" 2>/dev/null | sort -u | $2 "$3" | awk '{print $ 1}')
 		}
 	;;
+	SunOS)
+		SONAME_SUFFIX=".so"
+		function all_missing_libs_f () {
+			echo $(ldd "$1" 2>/dev/null | sort -u | sed -n 's/	\(.*\) =>	 (file not found)/\1/p')
+		}
+		function ldd_f () {
+			ldd "$1"
+		}
+		function required_libs_f () {
+			objdump -x $1 | grep NEEDED | awk '{print $ 2}' | tr '\n' ' ' | sed 's/ $//'
+		}
+		function search_lib_f () {
+			echo $(ldd "$1" 2>/dev/null | sort -u | $2 "$3" | awk '{print $ 1}')
+		}
+	;;
 esac
 
 # Update the incremental variables using /etc/profile.env, /etc/ld.so.conf,
