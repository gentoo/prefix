diff -ru gamin-0.1.9.orig/lib/gam_error.c gamin-0.1.9/lib/gam_error.c
--- gamin-0.1.9.orig/lib/gam_error.c	2008-03-20 13:45:56 +0100
+++ gamin-0.1.9/lib/gam_error.c	2008-03-20 11:28:21 +0100
@@ -95,7 +95,11 @@
 	/* if there is already an handler, leave it as is to
 	 * avoid disturbing the application's behaviour */
 	if (sigaction (SIGUSR2, NULL, &oldact) == 0) {
+#if defined(__INTERIX)
+	    if (oldact.sa_handler == NULL)
+#else
 	    if (oldact.sa_handler == NULL && oldact.sa_sigaction == NULL)
+#endif
 	        signal(SIGUSR2, gam_error_signal);
 	}
     }
diff -ru gamin-0.1.9.orig/libgamin/gam_error.c gamin-0.1.9/libgamin/gam_error.c
--- gamin-0.1.9.orig/libgamin/gam_error.c	2008-03-20 13:45:56 +0100
+++ gamin-0.1.9/libgamin/gam_error.c	2008-03-20 11:28:21 +0100
@@ -95,7 +95,11 @@
 	/* if there is already an handler, leave it as is to
 	 * avoid disturbing the application's behaviour */
 	if (sigaction (SIGUSR2, NULL, &oldact) == 0) {
+#if defined(__INTERIX)
+	    if (oldact.sa_handler == NULL)
+#else
 	    if (oldact.sa_handler == NULL && oldact.sa_sigaction == NULL)
+#endif
 	        signal(SIGUSR2, gam_error_signal);
 	}
     }
 
diff -ru gamin-0.1.9.orig/libgamin/gam_api.c gamin-0.1.9/libgamin/gam_api.c
--- gamin-0.1.9.orig/libgamin/gam_api.c	2008-03-20 13:45:56 +0100
+++ gamin-0.1.9/libgamin/gam_api.c	2008-03-20 13:45:07 +0100
@@ -21,6 +21,10 @@
 #include "gam_fork.h"
 #include "gam_error.h"
 
+#ifdef __INTERIX
+# include <sys/time.h>
+#endif
+
 #define TEST_DEBUG
 
 #define MAX_RETRIES 25
