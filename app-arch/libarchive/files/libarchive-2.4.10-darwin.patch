--- cpio/cpio.c
+++ cpio/cpio.c
@@ -530,7 +530,7 @@
 #ifdef HAVE_LUTIMES
         if (lutimes(name, times) != 0)
 #else
-        if (!S_ISLNK(a->mode) && utimes(name, times) != 0)
+        if (!S_ISLNK(cpio->mode) && utimes(name, times) != 0)
 #endif
                 cpio_warnc(errno, "Can't update time for %s", name);
 #endif
