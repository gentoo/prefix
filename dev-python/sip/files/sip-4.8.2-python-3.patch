--- sipdistutils.py
+++ sipdistutils.py
@@ -29,7 +29,7 @@
                     out.append(os.path.join(self.build_temp, o))
                 return out
 
-        raise RuntimeError, "cannot parse SIP-generated '%s'" % sbf
+        raise RuntimeError("cannot parse SIP-generated '%s'" % sbf)
 
     def _find_sip(self):
         import sipconfig
