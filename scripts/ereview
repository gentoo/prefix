#!/usr/bin/env bash
cd /tmp
patch=${1##*\/}
wget $1 -O $patch
less $patch
e=$(grep -o "/[^-]*-[^/]*/[^/]*/[^/]*.ebuild" $patch | head -n 1 | sed -e "s:^/::")
catpkg=${e%/*}
ebuild=${e##*/}
cd -
ecopy $catpkg $ebuild
cd $catpkg
patch -p1 < /tmp/$patch
cd ../../
ecommit $catpkg
