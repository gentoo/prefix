https://savannah.gnu.org/bugs/index.php?27846

--- xargs/xargs.c.orig	2009-10-29 11:22:09 +0100
+++ xargs/xargs.c	2009-10-29 12:08:27 +0100
@@ -438,9 +438,6 @@
        * specifies that it shall be at least LINE_MAX.
        */
       long val;
-#ifdef ARG_MAX
-      assert(bc_ctl.arg_max <= (ARG_MAX-2048));
-#endif
 #ifdef _SC_ARG_MAX
       val = sysconf(_SC_ARG_MAX);
       if (val > 0)
