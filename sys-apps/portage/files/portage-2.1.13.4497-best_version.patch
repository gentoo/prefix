--- bin/ebuild.sh
+++ bin/ebuild.sh
@@ -185,7 +185,7 @@
 	fi
 	# returns the best/most-current match.
 	# Takes single depend-type atoms.
-	@PORTAGE_BASE@/bin/portageq 'best_version' "${ROOT}" "$1"
+	@PORTAGE_BASE@/bin/portageq 'best_version' "${ROOT#${EPREFIX}}" "$1"
 }
 
 with_bindir() {
