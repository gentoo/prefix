Index: src/pv/file.c
===================================================================
--- src/pv/file.c	(revision 167)
+++ src/pv/file.c	(working copy)
@@ -80,8 +80,10 @@
 				opts->size += lseek64(fd, 0, SEEK_END);
 				close(fd);
 			}
+		} else if (S_ISREG(sb.st_mode)) {
+			opts->size += sb.st_size;
 		} else {
-			opts->size += sb.st_size;
+			opts->size = 0;
 		}
 	}
 }
