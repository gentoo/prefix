* the Darwin linker doesn't know an argument -z,now, I think it
  shouldn't be in here eiter

--- clients/ksu/Makefile.in
+++ clients/ksu/Makefile.in
@@ -9,7 +9,6 @@
 PROG_RPATH=$(KRB5_LIBDIR)
 
 KSU_LIBS=@KSU_LIBS@
-LAZY_LDFLAGS=-Wl,-z,now
 
 SRCS = \
 	$(srcdir)/krb_auth_su.c \
