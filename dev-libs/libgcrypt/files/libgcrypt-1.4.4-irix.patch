http://bugs.gentoo.org/show_bug.cgi?id=256652
https://bugs.g10code.com/gnupg/issue992

--- libgcrypt-1.4.4/cipher/Makefile.am.dist	2009-02-02 15:28:18.690218480 +0000
+++ libgcrypt-1.4.4/cipher/Makefile.am	2009-02-02 15:27:11.280311360 +0000
@@ -70,7 +70,7 @@ camellia.c camellia.h camellia-glue.c
 
 # We need to lower the optimization for this module.
 tiger.o: $(srcdir)/tiger.c
-	`echo $(COMPILE) -c $(srcdir)/tiger.c | sed -e 's/-O[2-9s]*/-O1/g' `
+	`echo $(COMPILE) -c $(srcdir)/tiger.c | sed -re 's/(^|[[:space:]])-O([2-9s])?([[:space:]]|$$)/ -O1 /g' `
 
 tiger.lo: $(srcdir)/tiger.c
-	`echo $(LTCOMPILE) -c $(srcdir)/tiger.c | sed -e 's/-O[2-9s]*/-O1/g' `
+	`echo $(LTCOMPILE) -c $(srcdir)/tiger.c | sed -re 's/(^|[[:space:]])-O([2-9s])?([[:space:]]|$$)/ -O1 /g' `
--- libgcrypt-1.4.4/cipher/Makefile.in.dist	2009-02-02 15:28:25.233899280 +0000
+++ libgcrypt-1.4.4/cipher/Makefile.in	2009-02-02 15:27:11.281058000 +0000
@@ -559,10 +559,10 @@ uninstall-am:
 
 # We need to lower the optimization for this module.
 tiger.o: $(srcdir)/tiger.c
-	`echo $(COMPILE) -c $(srcdir)/tiger.c | sed -e 's/-O[2-9s]*/-O1/g' `
+	`echo $(COMPILE) -c $(srcdir)/tiger.c | sed -re 's/(^|[[:space:]])-O([2-9s])?([[:space:]]|$$)/ -O1 /g' `
 
 tiger.lo: $(srcdir)/tiger.c
-	`echo $(LTCOMPILE) -c $(srcdir)/tiger.c | sed -e 's/-O[2-9s]*/-O1/g' `
+	`echo $(LTCOMPILE) -c $(srcdir)/tiger.c | sed -re 's/(^|[[:space:]])-O([2-9s])?([[:space:]]|$$)/ -O1 /g' `
 # Tell versions [3.59,3.63) of GNU make to not export all variables.
 # Otherwise a system limit (for SysV at least) may be exceeded.
 .NOEXPORT:
