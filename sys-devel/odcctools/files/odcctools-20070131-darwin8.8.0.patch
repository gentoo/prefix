--- ld64/src/MachOWriterExecutable.hpp
+++ ld64/src/MachOWriterExecutable.hpp
@@ -51,6 +51,16 @@
 #include "MachOFileAbstraction.hpp"
 
 
+// grobian@gentoo.org:
+// to get this compiling on Darwin 8.8.0 PPC, we need to define the
+// following two, I chose some wild guesses as values
+#ifndef x86_THREAD_STATE64_COUNT
+# define x86_THREAD_STATE64_COUNT 76
+#endif
+#ifndef x86_THREAD_STATE64
+# define x86_THREAD_STATE64 5
+#endif
+
 //
 //
 //	To implement architecture xxx, you must write template specializations for the following methods:
